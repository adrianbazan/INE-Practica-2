<tr>
  <td><%= link_to director.name, :action => 'show', :id => director %></td>
  <td><%= link_to 'Edit', :action => 'edit', :id => director %></td>
  <td>
    <%= button_to 'Delete', { :action => 'destroy', :id => director },
        data: { confirm: "Are you sure you want to delete director #{director.name}?" } %>   
  </td>
</tr>
